Selector de localidad: un `<select>` nativo de 48px de alto con las 20 localidades numeradas, para que el celular muestre su propio selector.

```jsx
<LocalitySelector value={loc} onChange={setLoc} hint="Los datos se actualizan al cambiar de localidad." />
```
